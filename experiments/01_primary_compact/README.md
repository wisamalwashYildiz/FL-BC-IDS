# Primary Compact Benchmark Evidence

This directory contains retained centralized and non-DP hierarchical references
plus the documented main configuration.

The previously curated `fl_bc_ids_dp` directory was reclassified under
`experiments/08_compact_security_evidence/` because its artifact tree is the
compact security-evidence workflow rather than the authoritative historical main
predictive-utility output bundle.

For statistically validated DP results on both datasets, see
`experiments/02_multiseed/`.
